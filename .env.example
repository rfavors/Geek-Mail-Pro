# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/geek_mail_pro
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=geek_mail_pro

# Authentication Configuration
SESSION_SECRET=your-super-secret-session-key-here
REPL_ID=your-repl-id
REPLIT_DOMAINS=your-domain.com
ISSUER_URL=https://replit.com/oidc

# Email Configuration
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Application Configuration
NODE_ENV=production
PORT=5000

# Optional: Custom Domain for Email Forwarding
CUSTOM_DOMAIN=thegeektrepreneur.com

# Optional: Webhook URL for email forwarding
WEBHOOK_URL=https://your-app.com/api/webhook/email